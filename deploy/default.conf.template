# Single origin: nginx serves the Svelte bundle and proxies /api to Litestar. The
# browser only ever sees one host, so no CORS and no API URL baked into the bundle.
# Frontend and API remain two containers, sized independently.
#
# A template, not a plain conf: the nginx entrypoint runs envsubst over
# /etc/nginx/templates at startup to inject ${API_KEY}. NGINX_ENVSUBST_FILTER limits
# the substitution to that one name, so nginx's own $uri/$host survive untouched.
# Keep the protocol seen by the *browser*. Traefik terminates TLS and talks to this
# container in plain HTTP, so $scheme is "http" here: forwarding it would tell the API
# the visitor is not on HTTPS, which breaks Secure cookies and absolute redirects.
# Falls back to $scheme when no proxy sits in front (local `docker compose`).
map $http_x_forwarded_proto $forwarded_proto {
    ""      $scheme;
    default $http_x_forwarded_proto;
}

# Pass the chain through untouched when a proxy already set it. The usual
# $proxy_add_x_forwarded_for would append $remote_addr, which real_ip has just
# rewritten to the client address — listing it twice for no reason.
map $http_x_forwarded_for $forwarded_for {
    ""      $remote_addr;
    default $http_x_forwarded_for;
}

# La racine nue n'est pas une page : elle renvoie vers une langue. Une
# redirection temporaire, pas permanente, parce que le choix dépend du visiteur
# et non du site. Le français est le défaut, l'anglais si le navigateur le
# demande en premier.
map $http_accept_language $langue_choisie {
    default  /fr;
    ~*^en    /en;
}

server {
    # 8080 rather than 80: the base image runs unprivileged.
    listen 8080;

    # Recover the real client address from the proxy in front, so $remote_addr — and
    # therefore the access log and X-Real-IP — stop showing Traefik's internal IP.
    # Trust is limited to private ranges: X-Forwarded-For is client-controlled and
    # must never be believed when it comes straight from the internet.
    set_real_ip_from 10.0.0.0/8;
    set_real_ip_from 172.16.0.0/12;
    set_real_ip_from 192.168.0.0/16;
    real_ip_header X-Forwarded-For;
    real_ip_recursive on;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Location relatif plutôt qu'absolu. Sinon nginx reconstruit l'adresse avec
    # son propre nom et son propre port, et derrière Traefik la redirection de
    # langue envoyait le visiteur sur 127.0.0.1:8080.
    absolute_redirect off;

    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/javascript application/json image/svg+xml;

    # Vite hashe le nom de chaque asset, donc le contenu d'une URL ne change jamais.
    location /assets/ {
        include /etc/nginx/security-headers.conf;

        # One header only: adding `expires` would emit a second, duplicate one.
        add_header Cache-Control "public, max-age=31536000, immutable" always;
        access_log off;
        try_files $uri =404;
    }

    location /api/ {
        # The API key is added here, server-side, so it never reaches the browser:
        # the bundle holds no secret. proxy_set_header also overwrites any header a
        # client sent, so this value is the only one the API ever sees from us.
        proxy_set_header X-API-Key "${API_KEY}";

        proxy_pass http://api:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $forwarded_for;
        proxy_set_header X-Forwarded-Proto $forwarded_proto;
    }

    # The OpenAPI docs, served by Litestar.
    location /schema {
        proxy_pass http://api:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $forwarded_for;
        proxy_set_header X-Forwarded-Proto $forwarded_proto;
    }

    # Asked for by browsers on their own. Without this it falls through to the SPA
    # fallback below and answers HTML with a 200, where an icon is expected.
    # robots.txt and sitemap.xml are generated from the request host, so they
    # follow whatever domain the site is served on. They sit at the root
    # because that is where a crawler looks for them.
    location ~ ^/(robots\.txt|sitemap\.xml)$ {
        proxy_pass http://api:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location = /favicon.ico {
        try_files $uri =404;
        access_log off;
    }

    # La mesure d'audience, relayée vers l'OpenPanel auto hébergé.
    #
    # Le SDK vise un chemin de même origine plutôt que le domaine d'OpenPanel :
    # la CSP reste `connect-src 'self'`, et un bloqueur qui filtre les domaines
    # connus n'a rien à filtrer, donc les chiffres ne manquent pas
    # silencieusement d'une part des visiteurs. Ce bloc est déclaré avant
    # `/api/` : nginx retient le préfixe le plus long, celui-ci gagne.
    location /api/op {
        proxy_pass ${OPENPANEL_API}/;
        proxy_http_version 1.1;
        proxy_set_header Host $proxy_host;
        proxy_set_header X-Forwarded-For $forwarded_for;
        proxy_set_header X-Forwarded-Proto $forwarded_proto;
        # Une mesure d'audience ne doit jamais faire attendre une page.
        proxy_connect_timeout 2s;
        proxy_read_timeout 5s;
        access_log off;
    }

    location = / {
        return 302 $langue_choisie;
    }

    # Le site est prérendu : chaque URL a son propre index.html, avec son titre,
    # sa description et son canonique. `$uri/index.html` sert ce fichier ; le
    # repli sur /index.html ne concerne plus que les URL inconnues, où
    # l'application affiche sa page 404.
    #
    # `no-cache` sur ce HTML, sinon un navigateur garderait les anciens noms
    # d'assets hachés et ne verrait jamais un déploiement.
    location / {
        include /etc/nginx/security-headers.conf;

        try_files $uri $uri/index.html =404;
        add_header Cache-Control "no-cache" always;
    }

    # Une URL inconnue rend l'application, qui affiche sa page 404, et nginx
    # garde le code 404 : `error_page` conserve le code d'origine, contrairement
    # à un `try_files ... /index.html` qui répondrait 200.
    #
    # Répondre 200 sur une page d'erreur est un « soft 404 » : l'index garde
    # l'URL, la classe comme un doublon de l'accueil, et le dépôt piighost-seo
    # verrait la courbe bouger sans en connaître la cause.
    error_page 404 /index.html;
}
